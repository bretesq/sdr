# Copy to .env and set for your machine.
# Hosts allowed to reach the dev server by NAME (bare IPs and localhost always
# work). Comma-separated; a leading dot matches the domain and its subdomains.
NUXT_ALLOWED_HOSTS=yourbox.example.com,.example.com
